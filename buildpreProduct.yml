version: 0.1
version: 0.2

phases:
  install:
    commands:
      - echo "Installing dependencies..."
      - npm install
  build:
    commands:
      - echo "Building project..."
      - npm run build
  test:
    commands:
      - echo "Running tests..."
      - npm test

artifacts:
  files:
    - '**/*'
  base-directory: 'dist'

